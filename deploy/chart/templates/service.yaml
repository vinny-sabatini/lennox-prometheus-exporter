apiVersion: v1
kind: Service
metadata:
  name: {{ include "lennox-prometheus-exporter.fullname" . }}
  labels:
    {{- include "lennox-prometheus-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lennox-prometheus-exporter.selectorLabels" . | nindent 4 }}
